import {StyleSheet} from 'react-native';

// export const Images = {
//   emptyIcon: require('../../assets/wishList/brand_wish_no.png'),
// };

export default StyleSheet.create({
  container: {
    backgroundColor: 'rgba(0,0,0,0)',
    borderWidth: 0,
  },
});
